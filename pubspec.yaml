name: devfest_2025_daegu
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  genui: ^0.6.0  # (가정) 최신 버전 확인
  genui_google_generative_ai: ^0.6.0
  json_schema_builder: ^0.1.3
  google_cloud_ai_generativelanguage_v1beta: ^0.3.0
  http: ^1.6.0
  protobuf: ^6.0.0
  gpt_markdown: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
